# Case Sheet Schema — Contract

Source of truth: `backend/app/models/case_sheet.py`

Har extracted field ek `Evidence` object hai:
```json
{ "value": "3 din", "evidence": "teen din se dard hai", "turn_index": 2 }
```

Ye schema change karne se pehle team ko batao — frontend aur extraction dono ispe depend
karte hain.
